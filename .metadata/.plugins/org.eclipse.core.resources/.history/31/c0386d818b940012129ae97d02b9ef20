package cs5530;

import java.util.Collection;

import models.IDbModel;


public class Helper {
	public static String tablesToString(Collection<IDbModel>... allTables){
		String results = "";
		for (Collection<IDbModel> tables : allTables)
			for (IDbModel table : tables)
				results += table.toString();
		return results;
	}
}
